const Router = require('express').Router

const router = Router();

// 容错处理
router.get("/h",(req,res)=>{
	res.json({

	});
});
// 容错处理
router.get("/t",(req,res)=>{
	res.json({

	});
});

module.exports = router;